interface:
  display_name: "Mestre do PC V10"
  short_description: "V10, launcher PowerShell, MCP Node e Ollama"
  default_prompt: "Use a skill do Mestre do PC V10 e preserve os limites de segurança do launcher elevado."
policy:
  allow_implicit_invocation: true
